using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class darovamir : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Вывод в консоли дарова мир
        print("Hello,World!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
